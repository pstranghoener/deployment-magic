name: Deploy Application

on:
  push:
    branches: [ "main" ]

jobs:
  prepare-release-on-servers:
    name: "Prepare release"
    runs-on: ubuntu-latest
    steps:
      - name: Upload
        uses: appleboy/scp-action@master
        with:
          host: p1234.mittwaldserver.info
          username: p1234
          port: 22
          key: ${{ secrets.SSH_KEY }}
          source: test.txt
          target: /files/test
